<h1>Manufacturers</h1>
<h5><%= link_to 'Add manufacturer', new_manufacturer_path %></h5>

<ul>
  <% @manufacturers.each do |manufacturer| %>
    <li>
      <%= link_to manufacturer.name, manufacturer_path(manufacturer) %> -
      <%= manufacturer.car_count %>
    </li>
  <% end %>
</ul>
